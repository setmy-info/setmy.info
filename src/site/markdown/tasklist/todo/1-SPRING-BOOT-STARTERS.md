
- [] Task: Make for eash setmy.info java modules into springboot-start-project intermediate parent pom module sub module by spring-boot-example-starter. Just prepare but do not add these to springboot-start-app dependencies. Only new modules are prepared by java-* projects. Make modules next to spring-boot-example-starter. Each starter module should have java module as dependency. Skip java-models, groovy-models, java-services, groovy-services, dependencies-bom, incubation things. Looks like java-* related modules. So in future  I can just add starter module and relevant java module is added.
