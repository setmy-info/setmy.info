(ns foo-bar-lib-probe.core-test
    (:require [clojure.test :refer :all]
              [foo-bar-lib-probe.core :refer :all]))

(deftest a-test
    (testing "FIXME, I fail."
             (is (= 1 1))))
